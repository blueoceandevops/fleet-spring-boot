spring:
  application:
    name: dubbo-consumer
server:
  port: 8080
dubbo:
  application:
    name: dubbo-consumer
  registry:
    protocol: zookeeper
    address: localhost:2181
    client: curator
  consumer:
    check: false
