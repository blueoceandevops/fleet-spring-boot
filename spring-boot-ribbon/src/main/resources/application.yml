spring:
  application:
    name: service-ribbon
  profiles:
    active: dev
server:
  port: 8080
ribbon:
  eureka:
    enabled: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    healthcheck:
      enabled: true
  instance:
    prefer-ip-address: true

